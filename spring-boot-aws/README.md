# Procedure (Amazon Linux instance + Spring Boto only)
- Initial script to run after creating instance 
- ```shell
     sudo yum update -y
     sudo amazon-linux-extras install java-openjdk11
     sudo yum install git
  ```